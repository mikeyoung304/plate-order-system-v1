# Import models from the _models.py file located in the parent directory (app/db/)
# This makes them available under the app.db.models namespace.
from .._models import FloorPlan, Table, Seat, Resident, Order